## What changed

<!-- One or two sentences. -->

## Why

<!-- What problem does this solve? Link the issue if there is one (`Fixes #123`). -->

## Test plan

- [ ] `pio run` for the affected target(s) — which? <!-- e.g. cores3, core2 -->
- [ ] Native unit tests pass (`pio test -e native`) — only if you touched `src/needs_logic.*`.
- [ ] Tested on real hardware — which board(s)?
- [ ] If UI-affecting, screenshot or short video attached.

## Notes

<!-- Anything reviewers should pay extra attention to? AI-assisted? Trailing TODOs? -->
